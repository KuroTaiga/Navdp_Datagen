# Source this file from scripts/massgen/run_envtest_chingmu_human_detached.sh.
#
# Active first-pass render families. Uncomment additional families when the
# human-only run is healthy and you want to expand the workload.
ACTIVE_MISSION_FAMILIES=(
  "deliver_to_human"
  "human_guided_uncertain_region"
  # "dense_dynamic_avoidance"
  # "dense_dynamic_humans"
  # "navigate_with_social_constraints:pedestrian_yield"
  # "navigate_with_social_constraints:queue_order"
  # "serve_queue"
)

ACTIVE_SOURCES=(
  "CHINGMU_rescaled_1"
  "CHINGMU_rescaled_2"
  "CHINGMU_rescaled_3"
  # "InteriorGS"
)

# Leave empty to use every selected scene from each active CHINGMU source.
# To narrow the run, uncomment scene IDs such as:
# ACTIVE_SCENES=("0052_858885" "0032_858992" "0011_859081")
ACTIVE_SCENES=()
